import React from 'react';

import { View } from 'react-native';

import styles from './Profile.json';

export default function Profile(props) {
    return (
        <View style={styles.container}>
        </View>
    );
}
